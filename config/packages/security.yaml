security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        App\Entity\User:
            algorithm: auto

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: username
        ldap_user_provider:
            id: App\Security\LdapUserProvider
        chain_provider:
            chain:
                providers: ['ldap_user_provider', 'app_user_provider']

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: chain_provider
            entry_point: form_login
            form_login_ldap:
                service: Symfony\Component\Ldap\Ldap
                dn_string: '%env(LDAP_BASE_DN)%'
                search_dn: '%env(LDAP_BIND_DN)%'
                search_password: '%env(LDAP_BIND_PASSWORD)%'
                query_string: '(%env(LDAP_UID_KEY)%={username})'
                login_path: login
                check_path: login
                default_target_path: dashboard
            form_login:
                login_path: login
                check_path: login
                default_target_path: dashboard
            logout:
                path: logout
                target: login

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: ^/login, roles: PUBLIC_ACCESS }
        - { path: ^/install, roles: PUBLIC_ACCESS }
        - { path: ^/assets/css/dynamic-theme\.css, roles: PUBLIC_ACCESS }
        - { path: ^/admin/users, roles: [ROLE_SYSTEM_ADMIN, ROLE_EMPLOYEES_VIEWER, ROLE_EMPLOYEES_EDITOR, ROLE_EMPLOYEES_MANAGER] }
        - { path: ^/admin, roles: [ROLE_SYSTEM_ADMIN] }
        - { path: ^/, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
