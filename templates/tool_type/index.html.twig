{% extends 'base.html.twig' %}

{% block title %}Typy narzędzi - {{ app_name() }}{% endblock %}
{% block page_title %}Typy narzędzi{% endblock %}

{% block breadcrumb %}
    <li class="breadcrumb-item"><a href="/">Strona główna</a></li>
    <li class="breadcrumb-item"><a href="/admin">Administracja</a></li>
    <li class="breadcrumb-item"><a href="{{ path('app_tool_index') }}">Narzędzia</a></li>
    <li class="breadcrumb-item active">Typy</li>
{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <div class="d-flex align-items-center">
                    <h5 class="card-title mb-0 flex-grow-1">Lista typów narzędzi</h5>
                    <div class="flex-shrink-0">
                        {% if can_create %}
                        <a href="{{ path('app_tool_type_new') }}" class="btn btn-success add-btn">
                            <i class="ri-add-line align-bottom me-1"></i> Dodaj typ
                        </a>
                        {% endif %}
                    </div>
                </div>
            </div>
            <div class="card-body">
                {% if types|length > 0 %}
                <div class="table-responsive">
                    <table class="table table-nowrap table-striped-columns mb-0">
                        <thead class="table-light">
                            <tr>
                                <th scope="col">Nazwa</th>
                                <th scope="col">Opis</th>
                                <th scope="col">Typ zarządzania</th>
                                <th scope="col">Liczba narzędzi</th>
                                <th scope="col">Status</th>
                                <th scope="col">Akcje</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for type in types %}
                            <tr>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <div class="flex-shrink-0 avatar-xs me-2">
                                            <div class="avatar-title rounded-circle bg-light text-body">
                                                {% if type.isMultiQuantity %}
                                                    <i class="ri-stack-line"></i>
                                                {% else %}
                                                    <i class="bx bx-package"></i>
                                                {% endif %}
                                            </div>
                                        </div>
                                        <div class="flex-grow-1">
                                            <h6 class="fs-14 mb-0">
                                                <a href="{{ path('app_tool_type_show', {id: type.id}) }}" class="text-body">{{ type.name }}</a>
                                            </h6>
                                        </div>
                                    </div>
                                </td>
                                <td>
                                    <span class="text-muted">{{ type.description|default('—') }}</span>
                                </td>
                                <td>
                                    {% if type.isMultiQuantity %}
                                    <span class="badge bg-info-subtle text-info">
                                        <i class="ri-stack-line me-1"></i>Wielosztuki
                                    </span>
                                    {% else %}
                                    <span class="badge bg-secondary-subtle text-secondary">
                                        <i class="bx bx-package me-1"></i>Pojedyncze
                                    </span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if type.tools|length > 0 %}
                                    <a href="{{ path('app_tool_index', {type: type.id}) }}" class="text-decoration-none">
                                        <span class="badge bg-primary-subtle text-primary">{{ type.tools|length }} narzędzi</span>
                                    </a>
                                    {% else %}
                                    <span class="text-muted">0 narzędzi</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if type.isActive %}
                                    <span class="badge bg-success-subtle text-success">Aktywny</span>
                                    {% else %}
                                    <span class="badge bg-danger-subtle text-danger">Nieaktywny</span>
                                    {% endif %}
                                </td>
                                <td>
                                    <div class="dropdown">
                                        <button class="btn btn-soft-secondary btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                            <i class="ri-more-fill align-middle"></i>
                                        </button>
                                        <ul class="dropdown-menu dropdown-menu-end">
                                            <li><a class="dropdown-item" href="{{ path('app_tool_type_show', {id: type.id}) }}">
                                                <i class="ri-eye-fill align-bottom me-2 text-muted"></i> Szczegóły
                                            </a></li>
                                            <li><a class="dropdown-item" href="{{ path('app_tool_index', {type: type.id}) }}">
                                                <i class="ri-list-check align-bottom me-2 text-muted"></i> Zobacz narzędzia
                                            </a></li>
                                            {% if can_edit %}
                                            <li><a class="dropdown-item edit-item-btn" href="{{ path('app_tool_type_edit', {id: type.id}) }}">
                                                <i class="ri-pencil-fill align-bottom me-2 text-muted"></i> Edytuj
                                            </a></li>
                                            {% endif %}
                                            {% if can_delete and type.tools|length == 0 %}
                                            <li class="dropdown-divider"></li>
                                            <li><a class="dropdown-item text-danger remove-item-btn" href="#" onclick="deleteType({{ type.id }}, '{{ type.name }}')">
                                                <i class="ri-delete-bin-fill align-bottom me-2"></i> Usuń
                                            </a></li>
                                            {% endif %}
                                        </ul>
                                    </div>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                {% else %}
                <div class="text-center py-5">
                    <div class="avatar-md mx-auto mb-4">
                        <div class="avatar-title bg-light rounded-circle display-6 text-muted">
                            <i class="bx bx-package"></i>
                        </div>
                    </div>
                    <h5>Brak typów</h5>
                    <p class="text-muted">Nie utworzono jeszcze żadnych typów narzędzi.</p>
                    {% if can_create %}
                    <a href="{{ path('app_tool_type_new') }}" class="btn btn-success">
                        <i class="ri-add-line me-1"></i> Dodaj pierwszy typ
                    </a>
                    {% endif %}
                </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<!-- Delete Modal -->
<div class="modal fade zoomIn" id="deleteRecordModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div class="mt-2 text-center">
                    <lord-icon src="https://cdn.lordicon.com/gsqxdxog.json" trigger="loop" colors="primary:#f7b84b,secondary:#f06548" style="width:100px;height:100px"></lord-icon>
                    <div class="mt-4 pt-2 fs-15 mx-4 mx-sm-5">
                        <h4>Czy jesteś pewien?</h4>
                        <p class="text-muted mx-4 mb-0">Czy na pewno chcesz usunąć typ "<span id="type-name-to-delete"></span>"?</p>
                    </div>
                </div>
                <div class="d-flex gap-2 justify-content-center mt-4 mb-2">
                    <button type="button" class="btn w-sm btn-light" data-bs-dismiss="modal">Anuluj</button>
                    <form method="post" id="delete-form" style="display: inline;">
                        <input type="hidden" name="_token" id="delete-token">
                        <button type="submit" class="btn w-sm btn-danger">Tak, usuń!</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block javascript %}
<script>
function deleteType(typeId, typeName) {
    document.getElementById('type-name-to-delete').textContent = typeName;
    document.getElementById('delete-form').action = '{{ path('app_tool_type_delete', {id: 'TYPE_ID'}) }}'.replace('TYPE_ID', typeId);
    
    // Generate CSRF token (you may need to implement this)
    document.getElementById('delete-token').value = 'csrf-token-here';
    
    new bootstrap.Modal(document.getElementById('deleteRecordModal')).show();
}
</script>
{% endblock %}